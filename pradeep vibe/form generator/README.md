# AI Vibe Form Generator

## Setup

1. **Backend**:
   - `cd server`
   - Create `.env` and add `GEMINI_API_KEY=your_key_here`
   - `node index.js`

2. **Frontend**:
   - `cd client`
   - `npm install`
   - `npm run dev`
